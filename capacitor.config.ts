import { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.sidhu69.profileaura',
  appName: 'Profile Aura',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
